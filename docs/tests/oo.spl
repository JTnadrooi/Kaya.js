<void>main(Int32~1): // OO programming test
	ext::writel("hello world!");
	new ext::[car]("civic", "honda")*|*->dump()|ext::writel("Hello ", *); // creates a new object at *1, calls it "dump" method and then prints it.
    new ext::[car]("civic", "honda")~|~->dump()|ext::writel("Hello ", ~); // same as above, the "~" automatically assigns a free pointer when set and gets the last set pointer when get.
