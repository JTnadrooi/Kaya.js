void: main(Int32~1); // entrypoint spl.
	ext::writel("hello world!"); // gets nothing as the pointer is pointing to memory of the line above, this gets cleared after every line.
	ext::writel(@1); // display the input variable. (given writel converts it to a string automatically)
	ext::get_value1()>1|ext::get_value2()>[1+1]|ext::writel(<1>, <2>);	// gets 2 values and displays them.
	ext::writel(<1>); // gets nothing as the pointer is pointing to memory of the line above, this gets cleared after every line.
	
