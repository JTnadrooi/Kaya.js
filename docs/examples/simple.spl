<void>main(): // entrypoint spl.
	ext::writel("hello world!"); // gets nothing as the pointer is pointing to memory of the line above, this gets cleared after every line.
	ext::writel(*); // display the input variable. (given writel converts it to a string automatically)
	// whiteline support of course.
	ext::get_value1()*|ext::get_value2()**|ext::writel(*, *2);	// gets 2 values and displays them.
	ext::writel(*); // gets nothing as the pointer is pointing to memory of the line above, this gets cleared after every line.
