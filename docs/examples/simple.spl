<void>main(): // entrypoint spl.
	ext::writel("hello world!"); // displays "hello world" using the writel method from the ext namespace.
	ext::get_value1()*|ext::get_value2()**|ext::writel(*, *2);	// gets 2 values and stores them inside the pointers, after which displays them using the same method as above.
	ext::writel(*); // linememory does not get cleared after each line, this will display whatever value is returned by ext::get_value1().
